const prefix = 'DEPARTMENT';

export const SET_DEPARTMENT = `${prefix}_SET_DEPARTMENT`;
export const SET_OPTIONS = `${prefix}_SET_OPTIONS`;
export const CLEAR_DEPARTMENT = `${prefix}_CLEAR_DEPARTMENT`;